# Modelagem de Software — CentralAI

> Disciplina: Padrões de Projeto de Software · UFG · INF  
> Referências: UML 2.5 · OMG Standard · Larman (2004) · Booch, Rumbaugh, Jacobson

---

## 1. Diagrama de Casos de Uso (Use Case Diagram)

Descreve **o que** o sistema faz do ponto de vista dos atores externos.

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            <<sistema>>                                   │
│                              CentralAI                                   │
│                                                                          │
│   (Explorar Catálogo)  ──────────────────────────────► Usuário           │
│   (Filtrar por Tipo)   ─────────────────────────────►  │                 │
│   (Buscar por Texto)   ─────────────────────────────►  │                 │
│   (Ver Detalhes da IA) ─────────────────────────────►  │                 │
│   (Favoritar IA)       ─────────────────────────────►  │                 │
│   (Comparar IAs)       ─────────────────────────────►  │                 │
│   (Receber Recomendação)────────────────────────────►  │                 │
│   (Acessar Chat IA)    ─────────────────────────────►  │                 │
│   (Escolher Plano)     ─────────────────────────────►  │                 │
│                                                         │                 │
│   (Gerenciar Catálogo) ──────────────────────────────► Administrador     │
│   (Configurar Plugins) ──────────────────────────────►  │                │
│                                                                          │
│   (Integrar via API)   ──────────────────────────────► Desenvolvedor     │
│   (Configurar Adapter) ──────────────────────────────►  │                │
└─────────────────────────────────────────────────────────────────────────┘

Relações entre casos de uso:
  (Filtrar por Tipo)  <<include>> (Explorar Catálogo)
  (Buscar por Texto)  <<include>> (Explorar Catálogo)
  (Ver Detalhes)      <<extend>>  (Explorar Catálogo)  [condição: clicou no card]
  (Favoritar IA)      <<extend>>  (Ver Detalhes)
  (Comparar IAs)      <<extend>>  (Explorar Catálogo)  [condição: selecionou 2+]
  (Acessar Chat IA)   <<include>> (Receber Recomendação) [generalização]
```

---

## 2. Diagrama de Classes — Camada de Domínio

Mostra a hierarquia de herança e as relações entre as classes.

```
┌──────────────────────────────────────────────────┐
│              <<abstract>> AITool                  │
│──────────────────────────────────────────────────│
│ - #name: string                                  │
│ - #company: string                               │
│ - #description: string                           │
│ - #pros: string[]                                │
│ - #cons: string[]                                │
│ - #rating: number                                │
│ - #apiAvailable: boolean                         │
│ + id: string                                     │
│ + pricing: PricingObj                            │
│ + tags: string[]                                 │
│ + emoji: string                                  │
│ + website: string                                │
│──────────────────────────────────────────────────│
│ + get name(): string                             │
│ + get isFree(): boolean   <<computed>>           │
│ + matches(query: string): boolean                │
│ + hasTag(tag: string): boolean                   │
│ + toJSON(): object                               │
│ + {static} getTypeInfo(ai): TypeInfo             │
│ + {static} filterByType(ais, type): AITool[]     │
└──────────────────┬───────────────────────────────┘
                   │ <<herança>>
         ┌─────────┼──────────────────────────┐
         ▼         ▼         ▼         ▼       ▼
    ┌─────────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐
    │ TextAI  │ │Image │ │ Code │ │Audio │ │Multimodal│
    │         │ │  AI  │ │  AI  │ │  AI  │ │    AI    │
    ├─────────┤ ├──────┤ ├──────┤ ├──────┤ ├──────────┤
    │#maxTokens││#resol│ │#lang.│ │#fmt  │ │#capabilit│
    │#languages││#style│ │#feat.│ │#maxD │ │ies       │
    └─────────┘ └──────┘ └──────┘ └──────┘ └──────────┘

┌──────────────────┐
│   AIFactory      │   <<Factory Method>>
├──────────────────┤
│ - #registry: Map │
├──────────────────┤
│+{static}create() │
│+{static}createAll│
└──────────────────┘

Decoradores (Decorator Pattern):
┌─────────────────┐         ┌──────────────────┐
│   AIDecorator   │────────►│     AITool        │
├─────────────────┤ wrapped └──────────────────┘
│ + get name()    │
│ + get company() │
│ + matches()     │
└────────┬────────┘
         │ <<herança>>
    ┌────┴──────┬────────────┬────────────────┐
    ▼           ▼            ▼                ▼
┌────────┐ ┌────────┐ ┌──────────┐ ┌──────────────┐
│Featured│ │  New   │ │ Verified │ │  OpenSource  │
│Decorat.│ │Release │ │Decorator │ │  Decorator   │
└────────┘ └────────┘ └──────────┘ └──────────────┘
```

---

## 3. Diagrama de Classes — Camada de Serviços

```
┌──────────────────────────────────┐
│         EventEmitter              │
├──────────────────────────────────┤
│ - _listeners: { [event]: fn[] }  │
├──────────────────────────────────┤
│ + on(event, listener): this      │
│ + off(event, listener): this     │
│ + emit(event, ...args): boolean  │
│ + removeAll(event): this         │
└──────────────────────────────────┘
           ▲ <<herda>>
           │
┌──────────────────────────────────────┐
│        RecommendationEngine           │   <<usa>> RecommendationStrategy
├──────────────────────────────────────┤
│ - #strategy: RecommendationStrategy  │
├──────────────────────────────────────┤
│ + setStrategy(strategy)              │
│ + recommend(ais, params, topN)       │
│ + get currentStrategyName()          │
└──────────────────────────────────────┘

┌──────────────────────────────┐
│  <<abstract>>                 │
│  RecommendationStrategy       │      <<Strategy Pattern>>
├──────────────────────────────┤
│ + recommend(ais, params): AI[]│
└──────────────┬───────────────┘
               │
    ┌──────────┼─────────────────┐
    ▼          ▼                 ▼                 ▼
┌────────┐ ┌────────┐ ┌───────────┐ ┌─────────────────┐
│  Task  │ │ Rating │ │   Cost    │ │    Balanced     │
│ Based  │ │ Based  │ │  Based    │ │    Strategy     │
└────────┘ └────────┘ └───────────┘ └─────────────────┘

┌───────────────────────────────────┐
│      StorageService               │   <<Singleton>>
├───────────────────────────────────┤
│ - {static} #instance: this       │
├───────────────────────────────────┤
│ + {static} getInstance(): this   │
│ + get(key, fallback): any        │
│ + set(key, value): void          │
│ + remove(key): void              │
│ + keys(): string[]               │
└───────────────────────────────────┘
```

---

## 4. Diagrama de Classes — Estruturas de Dados (core.js)

```
┌──────────────┐       ┌──────────┐      ┌───────────┐
│  LinkedList  │──────►│   Node   │      │   Stack   │
├──────────────┤ 0..*  ├──────────┤      ├───────────┤
│- #head: Node │       │+ data: T │      │- #items:[]│
│- #size: int  │       │+ next:Node│      ├───────────┤
├──────────────┤       └──────────┘      │+ push()   │
│+ prepend()   │                          │+ pop()    │
│+ append()    │       ┌──────────┐      │+ peek()   │
│+ remove()    │       │  Queue   │      └───────────┘
│+ contains()  │       ├──────────┤
│+ [Symbol.iter│       │- #items[]│      ┌──────────────┐
│  ator]()     │       │- #head:int      │ SearchIndex  │
└──────────────┘       ├──────────┤      ├──────────────┤
                        │+ enqueue()     │- #index: Map │
                        │+ dequeue()     ├──────────────┤
                        │+ peek()   │   │+ build(ais)  │
                        └──────────┘   │+ search(q)   │
                                        └──────────────┘

┌─────────────────┐      ┌──────────────────┐
│  CatalogIterator│      │   BinarySearch   │
├─────────────────┤      ├──────────────────┤
│- #items: T[]    │      │ (sem estado)     │
│- #index: int    │      ├──────────────────┤
│- #filter: fn    │      │+{static}search() │
├─────────────────┤      └──────────────────┘
│+ next()         │
│+ [Symbol.iter]()│
│+ toArray()      │
│+ skip(n)        │
│+ take(n)        │
└─────────────────┘
```

---

## 5. Diagrama de Componentes

Mostra os módulos do sistema e suas dependências em tempo de execução.

```
┌─────────────────────────────────────────────────────────────────┐
│                       index.html (SPA)                           │
│                      window.CentralAI.*                          │
└────────────────────────────┬────────────────────────────────────┘
                              │ <<usa>>
       ┌──────────────────────┼──────────────────────┐
       ▼                      ▼                       ▼
┌────────────┐       ┌──────────────┐        ┌────────────────┐
│  models.js │       │ services.js  │        │   core.js      │
│ ──────────  │       │ ──────────── │        │ ──────────────  │
│  Domain    │       │  Service    │        │  Data Structs  │
│  Layer     │       │  Layer      │        │  Algorithms    │
│            │       │             │        │  Advanced JS   │
│  AITool    │       │  EventEmit. │        │  Stack, Queue  │
│  TextAI    │       │  StorageSvc │        │  LinkedList    │
│  ImageAI   │       │  RecEngine  │        │  BinarySearch  │
│  CodeAI    │       │  Strategies │        │  SearchIndex   │
│  AudioAI   │◄──────│             │        │  Iterators     │
│  Multimod. │       └─────────────┘        │  Generators    │
│  AIFactory │                               │  Symbol/WeakMap│
└────────────┘                               │  Proxy         │
       ▲                                     └────────────────┘
       │ <<usa>>                                      ▲
       │                                              │ <<usa>>
┌──────────────┐                            ┌─────────────────┐
│ extensions.js│                            │  components.js  │
│ ──────────── │                            │ ─────────────── │
│  Patterns   │                            │  Presentation  │
│  Plugins    │                            │  Layer         │
│             │                            │                 │
│  Command    │                            │  Toast          │
│  Decorator  │◄───────────────────────────│  Modal          │
│  Adapter    │                            │  CatalogComp.   │
│  Chain      │                            │  Comparator     │
│  Plugins    │                            │  Recommender    │
│  ChatPlugin │                            │  StatsCounter   │
└──────────────┘                            └─────────────────┘
       ▲                                              ▲
       └────────────────────┬─────────────────────────┘
                            │ <<usa>>
                   ┌─────────────────┐
                   │    app.js       │
                   │ ─────────────── │
                   │  Bootstrap      │
                   │  App (Singleton)│
                   │  PluginManager  │
                   │  initPlugins()  │
                   └─────────────────┘

Dependências de dados:
  localStorage ◄──── StorageService (favorites, historico)
  DOM          ◄──── CatalogComponent, Modal, Toast, ChatPlugin
  CSS Custom   ◄──── ThemePlugin (--bg-page, --purple-600)
    Properties
```

---

## 6. Diagrama de Sequência — Favoritar uma IA

Mostra a interação entre objetos em ordem temporal.

```
Usuário    DOM(card)  CatalogComp  StorageService  Toast    EventEmitter
   │           │           │              │            │          │
   │ click     │           │              │            │          │
   │──────────►│           │              │            │          │
   │           │ dispatchEvent            │            │          │
   │           │──────────►│              │            │          │
   │           │  _handleCardClick(e)     │            │          │
   │           │  action='fav', id='...'  │            │          │
   │           │           │              │            │          │
   │           │           │ get('favorites',[])        │          │
   │           │           │─────────────►│            │          │
   │           │           │◄─────────────│ ids[]      │          │
   │           │           │              │            │          │
   │           │           │ [toggle logic]│            │          │
   │           │           │              │            │          │
   │           │           │ set('favorites', newIds)  │          │
   │           │           │─────────────►│            │          │
   │           │           │              │            │          │
   │           │ btn.innerHTML = '♥'      │            │          │
   │           │◄──────────│              │            │          │
   │           │           │              │            │          │
   │           │           │ Toast.success('Adicionado')          │
   │           │           │────────────────────────►  │          │
   │           │           │              │            │ show()   │
   │           │           │              │            │──────►   │
   │           │ toast visible             │            │          │
   │◄──────────│           │              │            │          │
   │           │           │              │            │          │
   [3s depois] │           │              │            │ auto-remove
               │           │              │            │──────────►
```

---

## 7. Diagrama de Sequência — Recomendar uma IA

```
Usuário   Formulário  RecommenderComp  RecEngine   Strategy   AI_DATABASE
   │          │              │              │            │           │
   │ submit   │              │              │            │           │
   │─────────►│              │              │            │           │
   │          │ FormData     │              │            │           │
   │          │─────────────►│              │            │           │
   │          │              │              │            │           │
   │          │              │ [valida taskType]         │           │
   │          │              │              │            │           │
   │          │              │ setStrategy(strategyMap[priority])   │
   │          │              │─────────────►│            │           │
   │          │              │              │            │           │
   │          │              │ recommend(AI_DATABASE, params, 3)    │
   │          │              │─────────────►│            │           │
   │          │              │              │ strategy.recommend()  │
   │          │              │              │────────────►│          │
   │          │              │              │            │ filter+sort
   │          │              │              │            │─────────►│
   │          │              │              │            │◄─────────│
   │          │              │              │◄───────────│           │
   │          │              │◄─────────────│ top3[]     │           │
   │          │              │              │            │           │
   │          │              │ render rec-cards          │           │
   │◄─────────────────────────│              │            │           │
   │ [3 cards visíveis]       │              │            │           │
```

---

## 8. Diagrama de Estados — Catálogo

Mostra os estados possíveis e as transições do componente de catálogo.

```
                    ┌────────────────┐
              ─────►│  Carregando    │
                    │  (Loading)     │
                    └───────┬────────┘
                            │ DOMContentLoaded
                            ▼
                    ┌────────────────┐
                    │  Exibindo Tudo │◄──── Limpar filtros
                    │  (12 IAs)      │
                    └───────┬────────┘
                            │
              ┌─────────────┼─────────────┐
              │             │             │
          Filtro         Busca       Filtro+Busca
              │             │             │
              ▼             ▼             ▼
    ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
    │ Filtrado por │ │ Resultado de │ │ Filtrado por │
    │    Tipo      │ │    Busca     │ │  Tipo+Busca  │
    └──────┬───────┘ └──────┬───────┘ └──────┬───────┘
           │                │                │
           └────────────────┼────────────────┘
                            │ resultado.length === 0
                            ▼
                    ┌────────────────┐
                    │  Sem resultados│
                    │  (Empty State) │
                    └────────────────┘
```

---

## 9. Diagrama de Atividades — Fluxo do Plugin de Chat

```
Início
  │
  ▼
[Usuário digita mensagem]
  │
  ▼
◇ mensagem.trim() vazia?
  ├── Sim ──► Fim (ignora)
  └── Não ──► [Adiciona mensagem 'user' ao histórico]
                │
                ▼
              [Exibe indicador 'digitando...']
                │
                ▼
              [Aguarda 800-1200ms (simula latência)]
                │
                ▼
              ◇ adapter configurado?
              ├── Sim ──► [fetch() real para API]
              │                 │
              │                 ▼
              │           [adapter.adapt(response)]
              │                 │
              └── Não ──► [mockReply(texto)]
                                │
                          ▼ (ambos convergem)
              [Adiciona mensagem 'assistant' ao histórico]
                │
                ▼
              [Re-renderiza mensagens]
                │
                ▼
              [Foca input]
                │
                ▼
              Fim
```

---

## 10. Diagrama de Pacotes — Arquitetura em Camadas

```
┌──────────────────────────────────────────────────────────┐
│                  <<package>> CentralAI                    │
│                                                          │
│  ┌───────────────────────────────────────────────────┐   │
│  │          <<layer>> Apresentação                   │   │
│  │  components.js · app.js (Chat, Catalog, Comparator│   │
│  │  Modal, Toast, Recommender, StatsCounter)         │   │
│  └──────────────────────┬────────────────────────────┘   │
│                          │ usa                            │
│  ┌───────────────────────▼────────────────────────────┐  │
│  │          <<layer>> Serviços                        │  │
│  │  services.js · extensions.js                      │  │
│  │  (EventEmitter, StorageService, RecEngine,        │  │
│  │   Strategies, Plugins, Command, Adapter, Chain)    │  │
│  └──────────────────────┬────────────────────────────┘  │
│                          │ usa                            │
│  ┌───────────────────────▼────────────────────────────┐  │
│  │          <<layer>> Domínio                         │  │
│  │  models.js (AITool, TextAI, ImageAI, CodeAI,      │  │
│  │             AudioAI, MultimodalAI, AIFactory)      │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
│  ┌────────────────────────────────────────────────────┐  │
│  │    <<layer>> Infraestrutura Transversal            │  │
│  │    core.js (Stack, Queue, LinkedList, Sort,        │  │
│  │             BinarySearch, SearchIndex, Iterator,   │  │
│  │             Generator, Symbol, WeakMap, Proxy)     │  │
│  └────────────────────────────────────────────────────┘  │
│                                                          │
└──────────────────────────────────────────────────────────┘

Dependências externas:
  localStorage ←── StorageService (persistência de favoritos)
  Google Fonts ←── CSS (Inter, JetBrains Mono)
  APIs de IA   ←── ChatPlugin + AIAPIAdapter (futuro)
```

---

## 11. Resumo dos Padrões de Projeto Implementados

| # | Padrão              | Categoria    | Arquivo           | Problema Resolvido                              |
|---|---------------------|--------------|-------------------|-------------------------------------------------|
| 1 | Singleton           | Criacional   | services.js       | Uma única instância de StorageService           |
| 2 | Factory Method      | Criacional   | models.js         | Criar TextAI/ImageAI/CodeAI sem if/else         |
| 3 | Observer            | Comportamental| services.js      | Desacoplar eventos entre componentes            |
| 4 | Strategy            | Comportamental| services.js      | Algoritmos de recomendação trocáveis            |
| 5 | Template Method     | Comportamental| models.js        | Contrato da hierarquia AITool → subclasses      |
| 6 | Module/Namespace    | Arquitetural | todos             | window.CentralAI evita poluição global          |
| 7 | Command             | Comportamental| extensions.js    | Undo/redo no comparador                         |
| 8 | Decorator           | Estrutural   | extensions.js     | Adicionar badges a IAs sem alterar classes      |
| 9 | Adapter             | Estrutural   | extensions.js     | Normalizar formatos de APIs diferentes          |
|10 | Chain of Responsibility| Comportamental| extensions.js | Pipeline de filtros do catálogo                |
|11 | Plugin              | Arquitetural | extensions.js     | Extensibilidade sem modificar o core            |

---

## 12. Análise de Complexidade (Big-O) das Operações Principais

| Operação                      | Estrutura       | Complexidade    | Observação                    |
|-------------------------------|-----------------|-----------------|-------------------------------|
| Filtrar catálogo por tipo     | Array.filter    | O(n)            | n = total de IAs              |
| Buscar por texto (linear)     | ai.matches()    | O(n × m)        | m = comprimento do query      |
| Buscar por texto (índice)     | SearchIndex     | O(k × t)        | k = tokens do query, t = índice |
| Ordenar por rating            | MergeSort       | O(n log n)      | estável                       |
| Favoritar (toggle)            | Array.includes  | O(n)            | n = favoritos salvos          |
| Stack push/pop                | Stack           | O(1)            | amortizado                    |
| Queue enqueue/dequeue         | Queue           | O(1)            | amortizado                    |
| LinkedList prepend            | LinkedList      | O(1)            | cabeça da lista               |
| Busca binária por ID          | BinarySearch    | O(log n)        | requer array ordenado         |
| Plugin register/activate      | Map.get/set     | O(1)            | chave = nome do plugin        |

---

*CentralAI © 2025 · UFG · Instituto de Informática · Padrões de Projeto de Software*
